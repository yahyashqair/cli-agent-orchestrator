# Review Task: Agent Provider Overrides Feature

## Scope
- Backend: new persistence/service layer (`services/agent_config_service.py`), DB migrations + API endpoints in `api/main.py`, provider precedence logic in `mcp_server/server.py`, CLI pre-check messaging in `cli/commands/launch.py`.
- UI: new settings component (`ui/src/components/AgentProviderSettings.tsx` + CSS), updates to `App.tsx`, `ControlPanel.tsx`, API client, and shared constants/types to surface per-agent provider overrides.
- Tests/Docs: additions under `test/api/test_agent_provider_configs_unit.py`, `test/services/...`, updates to existing MCP tests, docs (`README.md`, `ui/README.md`, `AGENTS.md`) summarizing configuration steps.

## What to look for
1. **Data integrity** – Ensure schema migration + service functions correctly upsert/delete configs and API validation enforces `constants.PROVIDERS`.
2. **Provider precedence** – Confirm `_create_terminal` (and any helper used elsewhere) resolves in the documented order: override > profile metadata > inherited provider > default.
3. **UI workflow** – Verify settings modal loads/saves overrides, integrates with Control Panel defaults, and handles errors.
4. **Tests** – Validate new tests cover success + error cases; ensure CI commands noted by developer align with repo guidelines.

## Deliverables
- Provide findings referencing impacted files/lines.
- Call out any missing tests or docs.
- Confirm whether feature is ready to merge or needs follow-ups.
