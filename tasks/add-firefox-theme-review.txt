Firefox Theme UI Update Review

Functionality
- ✅ `firefox` is threaded through every allowlist and selector so choosing it should persist and rehydrate correctly from localStorage and apply the CSS class (`/home/yahyashqair/anonDev/cli-agent-orchestrator/ui/src/App.tsx:20`, `/home/yahyashqair/anonDev/cli-agent-orchestrator/ui/src/App.tsx:27`, `/home/yahyashqair/anonDev/cli-agent-orchestrator/ui/src/App.tsx:80`, `/home/yahyashqair/anonDev/cli-agent-orchestrator/ui/src/components/ThemeToggle.tsx:27`).
- ✅ The stylesheet defines a complete `:root.theme-firefox` block that mirrors the token set used by other themes, so components reading the design tokens should pick up the palette without gaps (`/home/yahyashqair/anonDev/cli-agent-orchestrator/ui/src/styles/global.css:102`).

Readability
- ⚠️ The repeated `validThemes` arrays in state initialisers make the file harder to scan; a shared constant (and reuse in the dropdown) would keep the source of truth obvious (`/home/yahyashqair/anonDev/cli-agent-orchestrator/ui/src/App.tsx:20`, `/home/yahyashqair/anonDev/cli-agent-orchestrator/ui/src/App.tsx:27`, `/home/yahyashqair/anonDev/cli-agent-orchestrator/ui/src/components/ThemeToggle.tsx:27`).

Maintainability
- ⚠️ Having the theme identifiers listed in three places increases the chance of the next theme addition missing one spot; consider exporting a single `THEMES` array (and deriving the union type) to make future updates safer.

Performance
- ✅ Only static arrays and CSS variables are touched, so no performance concerns.

Security
- ✅ No new inputs or network calls; nothing concerning here.

Testing
- ⚠️ No automated coverage guards the theme list today. A simple assertion in a UI unit test (e.g., ensuring every theme constant is present in the toggle) would catch future omissions.

Documentation
- ℹ️ No documentation updates appear necessary for this UI-only addition.

Error Handling
- ✅ No error-handling paths were touched.

Validation & Follow-ups
- Developer noted `npm run lint` still fails due to existing violations in `ui/src/App.tsx` and `ui/src/components/TerminalViewer.tsx`; once those are addressed, re-running lint will confirm this change stays clean.
- Suggest a quick contrast check to verify the orange accent (`#ff7139`) remains accessible against the dark primary background (`#101820`) (`/home/yahyashqair/anonDev/cli-agent-orchestrator/ui/src/styles/global.css:104`, `/home/yahyashqair/anonDev/cli-agent-orchestrator/ui/src/styles/global.css:111`).
