Change Summary
- Added explicit terminal ID chips beside each terminal entry so operators can quickly map UI selections to backend identifiers.

Technical Details
- `ui/src/components/SessionList.tsx:227-242` introduces `terminal-profile-row` with a styled `terminal-id` span (including a helpful tooltip) so IDs display next to agent profiles in the session list.
- `ui/src/components/SessionList.css:169-195` defines flex layout adjustments plus capsule styling for the new ID chip, ensuring long IDs truncate gracefully while retaining a mono look for readability.

Open Items / Next Steps
1. Run `npm run dev` or `npm run build` under `ui/` to visually confirm the ID badges meet design expectations across themes.
2. If terminals can have extremely long IDs, consider a copy-to-clipboard button alongside the chip for admin workflows.
